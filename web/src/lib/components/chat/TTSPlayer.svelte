<script lang="ts">
  /**
   * Placeholder component for ElevenLabs playback. The actual text-to-speech logic
   * lives in the /api/tts endpoint and is triggered via fetch requests.
   * This component is kept to avoid breaking imports while the UI is refined.
   */
  export const placeholder = true;
</script>

<slot />
